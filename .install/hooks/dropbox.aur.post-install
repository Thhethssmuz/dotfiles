#!/bin/bash
set -euo pipefail

# fix for filesystem max-watchers
echo 'fs.inotify.max_user_watches = 100000' >> /etc/sysctl.d/99-sysctl.conf
sysctl --system

# hack to disable auto update
rm -rf ~/.dropbox-dist
install -dm0 ~/.dropbox-dist
